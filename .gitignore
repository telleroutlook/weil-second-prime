# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# proofctl — never commit private keys or CAS blobs
.proofctl/keys/*.priv
.proofctl/cas/
.proofctl/bundle/
.proofctl/snapshots/
.proofctl/env.json

# proofctl — commit these (attestations, graph, config, STATUS)
# .proofctl/attestations/ — tracked (signed attestations are proof artifacts)
# .proofctl/graph.json    — tracked
# .proofctl/config.json   — tracked

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo

# Serena (per-project cache/config, not proof artifacts)
.serena/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Logs
*.log
certs/
pilots/checkpoint-*.json
pilots/*-cache-*.checkpoint.json
pilots/s4_profile_checkpoint.json
